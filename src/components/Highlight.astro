---
interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<span class:list={["relative font-bold font-['Lobster']", className]}
  ><span class="absolute -z-1 top-[30%] left-0 w-full h-[40%] bg-accent/30 -rotate-3"></span><slot
  /></span
>
